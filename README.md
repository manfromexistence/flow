# Dx

Enhanced Development Experience.

Dx Security:
1. Shielded
2. Hexed
3. Cursed
4. Forbidden

Now in our tui we have to create an auto-suggestion list. Keep this in mind: the auto-suggestion can be local or remote.
- The local one will indicate all the command CLI options of our CLI itself.
- The remote suggestions mean that when we type anything it will use the Firefox Google auto-selections inside the Metasarge Auto Selection Rust created in our local folder and show search engine-like auto-selections from the remote.
It will be like a list that can be toggled using the top and bottom keys. When we click Enter it will use that suggestion.
Ok this is the tricky task and can be hard to do in one try so please ask me a clarification question if you need to know about anything. The main thing you want to know is just about the metasurge rust trade.
The metasurge rust trade inside deep nested has an auto-suggestion feature. Now we have to use that feature in our TUI so when we are writing any chat from our TUI input box, we will show a local CLI command solution or if it's not a local CLI command solution then we will show the browser search engine solutions in our TUI.
